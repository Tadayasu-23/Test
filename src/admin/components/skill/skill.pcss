.skill-component {
    display: flex;
    align-items: center;
    color: $text-color;
    margin-bottom: 15px;
  }
  
  .title {
    color: $text-color-alternate;
    font-size: 16px;
    flex: 1;
  }
  
  .buttons {
    display: flex;
    align-items: center;
  }
  
  .percent {
    color: $text-color-alternate;
    margin: 0 30px;
  }
  
  .btn {
    margin-right: 22px;
  
    &:last-child {
      margin-right: 0;
    }
  }